import { Box } from "@mui/material";

const SideBar = () => {
  return (
    <Box
      sx={{
        width: "4%",
        height: "100vh",
        bgcolor: "#e2e8f0",
        borderRadius: 999,
      }}
    />
  );
};

export default SideBar;
